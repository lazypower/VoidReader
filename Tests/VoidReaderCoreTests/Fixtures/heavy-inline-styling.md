<!--
  Shape: heavy-inline-styling
  Approx size: ~8KB
  Why: every paragraph loaded with bold/italic/code/link runs exercises inline-attribute merging and link-detection. Tuned to surface AttributedString construction cost and regex amplification.
-->

Paragraph **1** interleaves *italic* with `inline code`, links to [example](https://example.com/0), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-0) so the block never has a simple unstyled run.

Paragraph **2** interleaves *italic* with `inline code`, links to [example](https://example.com/1), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-1) so the block never has a simple unstyled run.

Paragraph **3** interleaves *italic* with `inline code`, links to [example](https://example.com/2), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-2) so the block never has a simple unstyled run.

Paragraph **4** interleaves *italic* with `inline code`, links to [example](https://example.com/3), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-3) so the block never has a simple unstyled run.

Paragraph **5** interleaves *italic* with `inline code`, links to [example](https://example.com/4), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-4) so the block never has a simple unstyled run.

Paragraph **6** interleaves *italic* with `inline code`, links to [example](https://example.com/5), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-5) so the block never has a simple unstyled run.

Paragraph **7** interleaves *italic* with `inline code`, links to [example](https://example.com/6), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-6) so the block never has a simple unstyled run.

Paragraph **8** interleaves *italic* with `inline code`, links to [example](https://example.com/7), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-7) so the block never has a simple unstyled run.

Paragraph **9** interleaves *italic* with `inline code`, links to [example](https://example.com/8), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-8) so the block never has a simple unstyled run.

Paragraph **10** interleaves *italic* with `inline code`, links to [example](https://example.com/9), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-9) so the block never has a simple unstyled run.

Paragraph **11** interleaves *italic* with `inline code`, links to [example](https://example.com/10), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-10) so the block never has a simple unstyled run.

Paragraph **12** interleaves *italic* with `inline code`, links to [example](https://example.com/11), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-11) so the block never has a simple unstyled run.

Paragraph **13** interleaves *italic* with `inline code`, links to [example](https://example.com/12), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-12) so the block never has a simple unstyled run.

Paragraph **14** interleaves *italic* with `inline code`, links to [example](https://example.com/13), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-13) so the block never has a simple unstyled run.

Paragraph **15** interleaves *italic* with `inline code`, links to [example](https://example.com/14), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-14) so the block never has a simple unstyled run.

Paragraph **16** interleaves *italic* with `inline code`, links to [example](https://example.com/15), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-15) so the block never has a simple unstyled run.

Paragraph **17** interleaves *italic* with `inline code`, links to [example](https://example.com/16), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-16) so the block never has a simple unstyled run.

Paragraph **18** interleaves *italic* with `inline code`, links to [example](https://example.com/17), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-17) so the block never has a simple unstyled run.

Paragraph **19** interleaves *italic* with `inline code`, links to [example](https://example.com/18), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-18) so the block never has a simple unstyled run.

Paragraph **20** interleaves *italic* with `inline code`, links to [example](https://example.com/19), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-19) so the block never has a simple unstyled run.

Paragraph **21** interleaves *italic* with `inline code`, links to [example](https://example.com/20), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-20) so the block never has a simple unstyled run.

Paragraph **22** interleaves *italic* with `inline code`, links to [example](https://example.com/21), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-21) so the block never has a simple unstyled run.

Paragraph **23** interleaves *italic* with `inline code`, links to [example](https://example.com/22), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-22) so the block never has a simple unstyled run.

Paragraph **24** interleaves *italic* with `inline code`, links to [example](https://example.com/23), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-23) so the block never has a simple unstyled run.

Paragraph **25** interleaves *italic* with `inline code`, links to [example](https://example.com/24), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-24) so the block never has a simple unstyled run.

Paragraph **26** interleaves *italic* with `inline code`, links to [example](https://example.com/25), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-25) so the block never has a simple unstyled run.

Paragraph **27** interleaves *italic* with `inline code`, links to [example](https://example.com/26), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-26) so the block never has a simple unstyled run.

Paragraph **28** interleaves *italic* with `inline code`, links to [example](https://example.com/27), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-27) so the block never has a simple unstyled run.

Paragraph **29** interleaves *italic* with `inline code`, links to [example](https://example.com/28), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-28) so the block never has a simple unstyled run.

Paragraph **30** interleaves *italic* with `inline code`, links to [example](https://example.com/29), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-29) so the block never has a simple unstyled run.

Paragraph **31** interleaves *italic* with `inline code`, links to [example](https://example.com/30), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-30) so the block never has a simple unstyled run.

Paragraph **32** interleaves *italic* with `inline code`, links to [example](https://example.com/31), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-31) so the block never has a simple unstyled run.

Paragraph **33** interleaves *italic* with `inline code`, links to [example](https://example.com/32), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-32) so the block never has a simple unstyled run.

Paragraph **34** interleaves *italic* with `inline code`, links to [example](https://example.com/33), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-33) so the block never has a simple unstyled run.

Paragraph **35** interleaves *italic* with `inline code`, links to [example](https://example.com/34), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-34) so the block never has a simple unstyled run.

Paragraph **36** interleaves *italic* with `inline code`, links to [example](https://example.com/35), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-35) so the block never has a simple unstyled run.

Paragraph **37** interleaves *italic* with `inline code`, links to [example](https://example.com/36), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-36) so the block never has a simple unstyled run.

Paragraph **38** interleaves *italic* with `inline code`, links to [example](https://example.com/37), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-37) so the block never has a simple unstyled run.

Paragraph **39** interleaves *italic* with `inline code`, links to [example](https://example.com/38), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-38) so the block never has a simple unstyled run.

Paragraph **40** interleaves *italic* with `inline code`, links to [example](https://example.com/39), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-39) so the block never has a simple unstyled run.

Paragraph **41** interleaves *italic* with `inline code`, links to [example](https://example.com/40), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-40) so the block never has a simple unstyled run.

Paragraph **42** interleaves *italic* with `inline code`, links to [example](https://example.com/41), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-41) so the block never has a simple unstyled run.

Paragraph **43** interleaves *italic* with `inline code`, links to [example](https://example.com/42), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-42) so the block never has a simple unstyled run.

Paragraph **44** interleaves *italic* with `inline code`, links to [example](https://example.com/43), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-43) so the block never has a simple unstyled run.

Paragraph **45** interleaves *italic* with `inline code`, links to [example](https://example.com/44), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-44) so the block never has a simple unstyled run.

Paragraph **46** interleaves *italic* with `inline code`, links to [example](https://example.com/45), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-45) so the block never has a simple unstyled run.

Paragraph **47** interleaves *italic* with `inline code`, links to [example](https://example.com/46), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-46) so the block never has a simple unstyled run.

Paragraph **48** interleaves *italic* with `inline code`, links to [example](https://example.com/47), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-47) so the block never has a simple unstyled run.

Paragraph **49** interleaves *italic* with `inline code`, links to [example](https://example.com/48), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-48) so the block never has a simple unstyled run.

Paragraph **50** interleaves *italic* with `inline code`, links to [example](https://example.com/49), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-49) so the block never has a simple unstyled run.

Paragraph **51** interleaves *italic* with `inline code`, links to [example](https://example.com/50), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-50) so the block never has a simple unstyled run.

Paragraph **52** interleaves *italic* with `inline code`, links to [example](https://example.com/51), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-51) so the block never has a simple unstyled run.

Paragraph **53** interleaves *italic* with `inline code`, links to [example](https://example.com/52), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-52) so the block never has a simple unstyled run.

Paragraph **54** interleaves *italic* with `inline code`, links to [example](https://example.com/53), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-53) so the block never has a simple unstyled run.

Paragraph **55** interleaves *italic* with `inline code`, links to [example](https://example.com/54), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-54) so the block never has a simple unstyled run.

Paragraph **56** interleaves *italic* with `inline code`, links to [example](https://example.com/55), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-55) so the block never has a simple unstyled run.

Paragraph **57** interleaves *italic* with `inline code`, links to [example](https://example.com/56), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-56) so the block never has a simple unstyled run.

Paragraph **58** interleaves *italic* with `inline code`, links to [example](https://example.com/57), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-57) so the block never has a simple unstyled run.

Paragraph **59** interleaves *italic* with `inline code`, links to [example](https://example.com/58), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-58) so the block never has a simple unstyled run.

Paragraph **60** interleaves *italic* with `inline code`, links to [example](https://example.com/59), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-59) so the block never has a simple unstyled run.

Paragraph **61** interleaves *italic* with `inline code`, links to [example](https://example.com/60), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-60) so the block never has a simple unstyled run.

Paragraph **62** interleaves *italic* with `inline code`, links to [example](https://example.com/61), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-61) so the block never has a simple unstyled run.

Paragraph **63** interleaves *italic* with `inline code`, links to [example](https://example.com/62), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-62) so the block never has a simple unstyled run.

Paragraph **64** interleaves *italic* with `inline code`, links to [example](https://example.com/63), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-63) so the block never has a simple unstyled run.

Paragraph **65** interleaves *italic* with `inline code`, links to [example](https://example.com/64), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-64) so the block never has a simple unstyled run.

Paragraph **66** interleaves *italic* with `inline code`, links to [example](https://example.com/65), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-65) so the block never has a simple unstyled run.

Paragraph **67** interleaves *italic* with `inline code`, links to [example](https://example.com/66), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-66) so the block never has a simple unstyled run.

Paragraph **68** interleaves *italic* with `inline code`, links to [example](https://example.com/67), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-67) so the block never has a simple unstyled run.

Paragraph **69** interleaves *italic* with `inline code`, links to [example](https://example.com/68), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-68) so the block never has a simple unstyled run.

Paragraph **70** interleaves *italic* with `inline code`, links to [example](https://example.com/69), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-69) so the block never has a simple unstyled run.

Paragraph **71** interleaves *italic* with `inline code`, links to [example](https://example.com/70), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-70) so the block never has a simple unstyled run.

Paragraph **72** interleaves *italic* with `inline code`, links to [example](https://example.com/71), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-71) so the block never has a simple unstyled run.

Paragraph **73** interleaves *italic* with `inline code`, links to [example](https://example.com/72), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-72) so the block never has a simple unstyled run.

Paragraph **74** interleaves *italic* with `inline code`, links to [example](https://example.com/73), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-73) so the block never has a simple unstyled run.

Paragraph **75** interleaves *italic* with `inline code`, links to [example](https://example.com/74), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-74) so the block never has a simple unstyled run.

Paragraph **76** interleaves *italic* with `inline code`, links to [example](https://example.com/75), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-75) so the block never has a simple unstyled run.

Paragraph **77** interleaves *italic* with `inline code`, links to [example](https://example.com/76), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-76) so the block never has a simple unstyled run.

Paragraph **78** interleaves *italic* with `inline code`, links to [example](https://example.com/77), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-77) so the block never has a simple unstyled run.

Paragraph **79** interleaves *italic* with `inline code`, links to [example](https://example.com/78), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-78) so the block never has a simple unstyled run.

Paragraph **80** interleaves *italic* with `inline code`, links to [example](https://example.com/79), **bold _nested_ italic**, and ~~strikethrough~~ fragments. A second sentence doubles the density with `a() + b()` and [another reference](#anchor-79) so the block never has a simple unstyled run.

